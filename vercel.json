{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @workspace/leadbridge-landing run build:vercel",
  "outputDirectory": "artifacts/leadbridge-landing/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
